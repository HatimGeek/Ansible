---
-
  gather_facts: false
  hosts: win
  tasks:
    -
      name: Ntp Config
      win_command: "{{ item }}"
      with_items:
        - w32tm /query /peers
        - tzutil /g
        - w32tm /stripchart /computer:localhost /samples:0
